{
  "mcpServers": {
    "gemini-image": {
      "command": "python3",
      "args": ["${CLAUDE_PLUGIN_ROOT}/servers/gemini-image/server.py"],
      "env": {
        "GEMINI_API_KEY": "${GEMINI_API_KEY}",
        "GEMINI_OUTPUT_DIR": "${GEMINI_OUTPUT_DIR}"
      }
    }
  }
}
